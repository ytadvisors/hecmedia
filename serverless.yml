name: ${env.APP_NAME}
timeout: 30

nextApp:
  component: "serverless-next.js"
  inputs:
    env:
      APOLLO_CLIENT_URI : ${env.APOLLO_CLIENT_URI}
    domain:
      - ${env.SUBDOMAIN}
      - "hecmedia.org"
    memory: 1024