name: ${env.APP_NAME}

nextApp:
  component: "serverless-next.js"
  timeout: 30
  inputs:
    env:
      APOLLO_CLIENT_URI : ${env.APOLLO_CLIENT_URI}
    domain:
      - ${env.SUBDOMAIN}
      - "hecmedia.org"
    memory: 1024