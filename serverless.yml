name: ${env.APP_NAME}
stage: ${env.ACTIVE_ENV}

nextApp:
  component: "serverless-next.js"
  inputs:
    env:
      APOLLO_CLIENT_URI : ${env.APOLLO_CLIENT_URI}
    domain:
      - ${env.SUBDOMAIN}
      - "hecmedia.org"